/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.pexels.com",
      "images.unsplash.com",
      "encrypted-tbn0.gstatic.com",
      "upload.wikimedia.org",
    ],
  },
};

module.exports = nextConfig;
